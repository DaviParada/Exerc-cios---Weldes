#include <stdio.h>
#include <stdbool.h> // Inclui a biblioteca stdbool.h para obter os limites dos tipos primitivos
int main (){
    bool condicao = true;  // Declara uma variável booleana 'condicao' e a inicializa com o valor true (verdadeiro)
    if (condicao) // Verifica se a condição é verdadeira
    printf ("A condição é verdadeira\n"); // Se for verdadeira, imprime "A condição é verdadeira"

}

